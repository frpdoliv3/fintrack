services:
    db:
        image: "postgres:16.3"
        restart: always
        environment:
            POSTGRES_USER: postgres
            POSTGRES_PASSWORD: example
            POSTGRES_DB: fintrack
        ports:
            - 5432:5432
